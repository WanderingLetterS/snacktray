//returns a positive integer when it's the project's anniversary
var date;

date=date_current_datetime()
if (date_get_month(date)=10 && date_get_day(date)=10) return (date_get_year(date)-2010)

return 0
