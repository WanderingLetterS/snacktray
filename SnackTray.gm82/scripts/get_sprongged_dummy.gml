///get_sprongged_dummy([vspeed],[hspeed])
//did any carbon-based life form say spring?

var i;

with (gravitymanager) if (carry=other.id) instance_destroy()

i=instance_create(x,y,gravitymanager)

i.carry=id
if (argument_count) i.vsp=argument[0]
else i.vsp=-7

springsmoke(x,y+8)
