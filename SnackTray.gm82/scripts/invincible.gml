///invincible(player)
//returns whether player is completely invincible

if (argument_count) with (argument[0]) return invincible()
return super || star || insta || misfire || hurt || flash || piped=1 || firedash
